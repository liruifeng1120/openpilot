# 快速参考卡 - 跟车抖动优化

## 一句话总结
**16年本田思域高速跟车抖动 = 雷达视觉权重硬切换 → 相对速度跳变 → 加速度突变**

---

## 5大优化方案速查表

```
优先级  方案           文件位置              改动难度  见效时间  效果
────────────────────────────────────────────────────────────────
🔴 1   平滑权重切换    radard.py L316-328    ★☆☆☆☆   即时      80%
🔴 2   距离滤波        radard.py L305-320    ★☆☆☆☆   即时      20%
🟡 3   自适应门槛      radard.py L127-140    ★★☆☆☆   1周       10%
🟡 4   降低MPC敏感度   long_mpc.py L426-432  ★★☆☆☆   1周       15%
🟢 5   利用不确定性    radard.py L310-328    ★★★☆☆   2周       5%
```

---

## 关键数字

| 参数 | 原值 | 改值 | 说明 |
|------|------|------|------|
| `cnt < ？` | 20帧 | 20-40帧(S曲线过渡) | 平滑权重切换 |
| `prob < ？` | 0.97 | 0.97(但用过渡因子) | 保持不变，用系数调节 |
| `abs(dist) > ？` | 5.0m | 10.0m | 增加容忍度 |
| `A_CHANGE_COST` | 250 | 500 | 降低加速度切换频率 |
| `jLead响应` | [0.3,2.0]→[250,20] | [0.2,1.5]→[500,150] | 更平和的响应 |
| `model_weight` | 直接0/1 | 0-1平滑过渡 | 关键改动 |

---

## 代码核心片段

### 片段1：平滑过渡函数
```python
def _smooth_step_function(cnt):
    if cnt < 20:
        return 0.0
    elif cnt >= 40:
        return 1.0
    else:
        p = (cnt - 20) / 20
        return p * p * (3 - 2 * p)  # S曲线
```

### 片段2：改进权重计算
```python
transition = _smooth_step_function(self.cnt)
base_weight = np.interp(self.prob, [0.97, 1.0], [0.4, 0.0])
final_weight = base_weight * transition  # 关键：乘以过渡因子
```

### 片段3：距离滤波
```python
dRel_raw = float(lead_msg.x[0]) - RADAR_TO_CAMERA
distance_filter.x = distance_filter.x * 0.95 + dRel_raw * 0.05
self.dRel = distance_filter.x
```

---

## 问题诊断流程

```
发现跟车抖动
    ↓
运行诊断脚本: python follow_car_diagnostics.py
    ↓
查看报告输出:
    ├─ 加速度标准差 > 0.3? → 优先级1、2
    ├─ Jerk标准差 > 0.5? → 优先级4
    ├─ 距离跳变 > 5次/秒? → 优先级2、3
    ├─ 权重-加速度关联 > 30%? → 优先级1
    └─ 相对速度跳变 > 0.5m/s? → 优先级1、2
    ↓
按优先级实施改动
    ↓
路测验证效果
```

---

## 实施清单

- [ ] 理解问题：阅读 analysis_report.md
- [ ] 实施方案1：平滑权重切换 (radard.py)
- [ ] 实施方案2：距离滤波 (radard.py)
- [ ] 编译/重启系统
- [ ] 第一轮路测（30分钟）
  - [ ] 检查是否有明显改善
  - [ ] 记录数据用于分析
- [ ] 数据分析：运行诊断脚本
- [ ] 如需继续优化，实施方案3和4
- [ ] 第二轮路测（1小时）
- [ ] 最终验证

---

## 常见错误

❌ **错误1**：直接改 `prob < 0.97` 的阈值  
✅ **正确**：保持阈值不变，用 `transition_factor` 系数调节

❌ **错误2**：一次性应用所有5个优化  
✅ **正确**：先做1和2（快速见效），再根据情况做3和4

❌ **错误3**：距离滤波时常数设置太大(>0.2s)  
✅ **正确**：0.05s左右最优（5个采样周期）

❌ **错误4**：增加A_CHANGE_COST到1000以上  
✅ **正确**：500已经足够，过大会导致响应迟钝

❌ **错误5**：修改了代码但没有清除缓存  
✅ **正确**：改完代码后清除 `__pycache__` 并重新启动系统

---

## 预期结果对比

### 改进前
```
加速度波形：
 1.0 |  ╱╲      ╱╲      ╱╲
 0.5 | ╱  ╲    ╱  ╲    ╱  ╲
 0.0 |____╲____╱____╲____╱____
-0.5 |     ╱   \     ╱
-1.0 |    ╱      ╲   ╱
      └────────────────────
      频繁波动，周期~0.5秒，峰值±1.0 m/s²
```

### 改进后
```
加速度波形：
 0.5 |   ___    ___
 0.2 |  /   \  /   \
 0.0 |_/     \/     \___
-0.2 |  \  /  \  /
     |   \/    \/
      └────────────────────
      平滑过渡，周期~2.0秒，峰值±0.2 m/s²
```

---

## 关键文件速查

| 文件 | 用途 | 何时查看 |
|------|------|--------|
| analysis_report.md | 问题深度分析 | 理解原因 |
| implementation_guide.md | 详细实现步骤 | 具体实施 |
| follow_car_optimization.py | 改进代码模块 | 参考实现 |
| follow_car_diagnostics.py | 诊断分析工具 | 验证效果 |
| OPTIMIZATION_SUMMARY.md | 完整总结 | 整体了解 |

---

## 参数微调建议

如果改进效果不理想：

### 情况1：仍有轻微抖动
```python
# 尝试延长过渡时间
TRANSITION_START = 30  # 从20改为30
TRANSITION_END = 60    # 从40改为60
```

### 情况2：远距离准度不足
```python
# 增加远距离的门槛宽度
dist_error_pct = np.interp(v_ego, [15, 40], [0.20, 0.40])  # 从[0.15,0.35]改为[0.20,0.40]
```

### 情况3：响应变迟钝
```python
# 降低A_CHANGE_COST
A_CHANGE_COST = 350  # 从500改为350
```

### 情况4：高速波动加重
```python
# 增加滤波时常数
time_constant=0.1  # 从0.05改为0.1
```

---

## 性能基准

成功优化的判断标准（任选2个达到）：

- ✅ 加速度标准差 < 0.2 m/s² (原为0.4-0.5)
- ✅ Jerk标准差 < 0.3 m/s³ (原为0.8-1.0)
- ✅ 距离波动 < 0.3m (原为±0.5m)
- ✅ 驾驶员主观感受明显改善

---

## 应急方案

如果改动导致系统不稳定：

1. **快速回滚**：注释掉 `transition_factor` 相关代码
2. **保守方案**：只保留方案2（距离滤波），暂不做权重过渡
3. **渐进方案**：延长过渡时间 TRANSITION_START=50, TRANSITION_END=100

---

**关键要点总结**：
1. 🎯 这不是传感器问题，是算法逻辑问题
2. 🎯 权重切换的平滑过渡是最关键的改动 (占80%效果)
3. 🎯 改动涉及2个文件，共4处主要修改
4. 🎯 改完立即见效，无需重新标定任何传感器
5. 🎯 所有改动都向后兼容，不会破坏其他功能

